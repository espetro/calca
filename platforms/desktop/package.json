{
  "name": "@app/electrobun",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web-dev": "bun run --cwd ../../apps/web dev",
    "electrobun-watch": "electrobun dev --watch",
    "dev:desktop": "bun run --parallel web-dev electrobun-watch",
    "prebuild:desktop": "rm -rf build artifacts Resources/web",
    "build:desktop": "bun run scripts/build.ts",
    "verify": "bun run scripts/verify.ts",
    "clean": "rm -rf build artifacts Resources/web",
    "validate": "bun ../../scripts/validate.ts --cwd $INIT_CWD"
  },
  "dependencies": {
    "@app/server": "workspace:*",
    "@logtape/file": "^2.0.5",
    "@logtape/logtape": "^2.0.5",
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@tailwindcss/browser": "4.2.4",
    "@types/bun": "^1.3.13",
    "@types/fs-extra": "^11",
    "fs-extra": "^11.3.4",
    "typescript": "^6.0.3"
  }
}