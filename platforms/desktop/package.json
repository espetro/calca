{
  "name": "@app/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run --cwd ../.. typecheck --filter=@app/desktop",
    "package": "electrobun package"
  },
  "dependencies": {
    "@app/server": "workspace:*",
    "electrobun": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
