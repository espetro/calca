{
  "name": "@app/electrobun",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "watch": "electrobun dev --watch",
    "dev:desktop": "bun run scripts/watch.ts",
    "build": "electrobun build --env=stable",
    "clean": "rm -rf build artifacts Resources/web"
  },
  "dependencies": {
    "@app/server": "workspace:*",
    "electrobun": "^1.16.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}